/**
 *  Checks if there is a legit username from POST to make a change it's password, then redirrect to /
 */
 module.exports= function(objectRepository){

    return function (req, res, next){
        return next();
    }
}